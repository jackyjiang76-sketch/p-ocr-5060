# p-ocr 技能配置说明（PaddleOCR-VL-1.6）

> 2026-08-26 实测版本。**后续只用 p-ocr（含复核）；OCR 预处理统一 200 DPI。o-ocr 目录/单元/进程已彻底清理删除。**
>
> **重大更新**：已把 Windows venv 的 Paddle 换为 **cu129 (paddlepaddle-gpu 3.3.1)**，**PaddleOCRVL native 管线（截图全套 use_* 参数）在 RTX 5060 (sm_120) 上真正 GPU 跑通**（详见 §2.3 native 管线）。vLLM 服务器路径（8001）与 native 管线**并存**，互不影响。

## 1. 现状（已配置完成并验证）

| 项目 | 状态 |
|------|------|
| 模型 | PaddleOCR-VL-1.6 (0.9B)，权重 WSL `/root/.tools/p-ocr/model`（1.83 GB bf16，完整） |
| 推理引擎 | vLLM 0.22.1（**独立 venv** `/root/.tools/p-ocr/.venv/bin/vllm`，不再依赖 o-ocr），PaddleOCR-VL 官方加速路径 |
| 服务 | systemd 单元 `p-ocr-serve`，端口 **8001**，`POST /v1/chat/completions`（OpenAI 兼容） |
| 服务状态 | **healthy（/health 200）**，已 enable 开机自启 |
| 客户端 | `C:\Users\justin\.tools\p-ocr\p_ocr_client.py`（已加 LOC token 清洗） |
| 图形卡 | RTX 5060 8GB（sm_120） |

> **独立 venv 说明**：p-ocr 已有自己的 `/root/.tools/p-ocr/.venv`（Python 3.13，vllm 0.22.1 + torch 2.11.0 + transformers 5.15.1 + flashinfer 0.6.11.post2 + nvidia cu13）。`p-ocr-serve.service` 的 `ExecStart`/`CUDA_HOME` 均指向它，**完全不依赖 o-ocr 目录**（o-ocr 目录/unit/进程已删除清理，释放约 25G）。已在独立 venv 下验证：健康 200 + 端到端 OCR 跑通。
>   - **关键坑（独立 venv 首次启动必踩）**：flashinfer 的 JIT 编译在**链接**阶段会因 `cu13` 下有 `lib` 但**没有 `lib64` 目录**而失败（`ld: cannot find -lcudart / -lcuda`）。要用 `uv` 装 nvidia-cu13 后，为 `/root/.tools/p-ocr/.venv/.../nvidia/cu13/lib64/`（需创建）建立两个链接：`libcuda.so -> /usr/lib/wsl/lib/libcuda.so.1`、`libcudart.so -> .../nvidia/cu13/lib/libcudart.so.13`（绝对路径）。否则 EngineCore 报 `Engine core initialization failed`、systemd 反复重启。
>   - 另两个补丁：`cuda_toolkit.h` 的 #error 已注释（nvcc 13.3 vs 头文件 13.0）；`lib` 下的 `libcudart.so`/`libcuda.so` 链接。（用 `patch_flashinfer.sh` 参考，但链接目录要是 **lib64**。）

## 2. 为什么不用 o-ocr / Windows 原生路径（重要）

1. **显存冲突（已解决）**：o-ocr(8000) 和 p-ocr(8001) 两个 vLLM 服务不能在这块 8GB 卡上同时满血跑，各 0.4 的 gpu-memory-utilization 叠加后 OOM（`No available memory for the cache blocks`），p-ocr 冷启动失败、端口 8001 一直绑不上。→ 已**彻底停掉并删除 o-ocr**（进程 + 自启 + 单元 + 目录），让 p-ocr 独占显卡。
   - **坑（重要）**：`systemctl disable --now o-ocr-serve` **只停单元管理 + 取消自启**，o-ocr 其实是孤儿子进程（ppid=1，环境 `/root/paddle3env`），**不会随 disable 被杀**，仍占 8000 端口和显存。**必须再 `kill -9 <pid>` 杀掉进程**才算彻底停。已实测：kill 后 8000 无监听、进程消失、显存只留 p-ocr。
2. **Windows 原生 Paddle — 已解决（关键）**：**cu126 版 paddle wheel 不支持 sm_120**（编译目标 75/80/86/89，RTX 5060 报 `Mismatched GPU Architecture`）。**解决办法：把 Windows venv 的 paddlepaddle-gpu 从 3.2.1(cu126) 升到 3.3.1(cu129)**——cu129 源支持 Blackwell/sm_120（Paddle 官方架构表：sm_120 用 CUDA 12.9）。升级后 `PaddleOCRVL` native 管线 GPU 跑通（gpu_count 1、matmul 正常、显存 7.4GB）。

### 2.3 PaddleOCR native 管线（截图全套参数，2026-08-26 实测通过）

Windows venv：`C:\Users\justin\.tools\p-ocr\.venv`（paddle 3.3.1 + paddleocr 3.7.0 + paddlex 3.7.2，已支持 sm_120）。

```python
from paddleocr import PaddleOCRVL
pipe = PaddleOCRVL(
    pipeline_version="v1.6",
    use_doc_orientation_classify=True,
    use_doc_unwarping=True,
    use_layout_detection=True,
    use_chart_recognition=True,
    use_seal_recognition=True,
    use_ocr_for_image_block=True,
    format_block_content=True,
    merge_layout_blocks=True,
)
output = pipe.predict("image.png")
for res in output:
    res.save_to_markdown(save_path="out")
```

> **性能问题（已定位与解决）**：native 纯跑很慢（一张 200dpi 图 **~126s**，最多 8 分钟）。已定位：**瓶颈不在图大小、不在预处理环节、不在 max_new_tokens/max_pixels/温度**（限制这些都没提速）——是 **PaddleOCR-VL 模型在 native（飞桨原生 eager）下逐 token 生成**本身慢。

### 2.4 推荐：native + vLLM 混合管线（22s/张，截图全套能力 + 正常速度）

**结论：native 预处理（截图全套 use_*）+ VL 识别交给 vLLM 服务器(8001)。** 实测 **22.2s/张**（vs native 纯跑 126s），快约 6 倍，输出质量不变。

```python
from paddleocr import PaddleOCRVL
pipe = PaddleOCRVL(
    pipeline_version="v1.6",
    use_doc_orientation_classify=True,   # 方向分类
    use_doc_unwarping=True,              # 畸变矫正
    use_layout_detection=True,           # 版面检测(PP-DocLayoutV3)
    use_chart_recognition=True,          # 图表
    use_seal_recognition=True,           # 印章
    use_ocr_for_image_block=True,        # 图片块OCR
    format_block_content=True,
    merge_layout_blocks=True,
    vl_rec_backend="vllm-server",        # 关键：VL 走 vLLM 而非 native
    vl_rec_server_url="http://127.0.0.1:8001/v1",
    vl_rec_api_model_name="paddleocr-vl-1.6",  # 必须与 vLLM served-model-name 一致
)
output = pipe.predict("image.png")
for res in output:
    res.save_to_markdown(save_path="out")
```

- **前提**：WSL 里 `p-ocr-serve`(vLLM, 8001) 运行中（模型名 `paddleocr-vl-1.6`）。Windows 通过 `http://127.0.0.1:8001/v1` 访问。
- **坑**：`vl_rec_api_model_name` 必须和 vLLM `/v1/models` 里的模型名一致（native 默认请求 `PaddleOCR-VL-1.6-0.9B`，与 vLLM 的 `paddleocr-vl-1.6` 不一致会 404）。
- 成品脚本：`pocr_hybrid.py`（`python pocr_hybrid.py <img.png> [outdir]`）。

- 模型自动下载到 `C:\tmp\pocr_pd\models\official_models`（PP-DocLayoutV3、PaddleOCR-VL-1.6、PP-LCNet_x1_0_doc_ori、UVDoc 等，缓存约 2GB，已就绪）。
- **实测结果**：人防建筑施工图设计说明 PDF（200dpi，6700×4756）→ Markdown 19935 字节，正确提取设计依据/GB 规范编号等。
- **性能注意**：首次全管线跑一张 200dpi 大图约 26 分钟（首次加载全模型 + 超大图 + 全管线逐级处理）。模型已缓存，后续会比首次快；**确需提速可降输入分辨率**（<2000px 宽）或调小采样。
- 验证 vLLM p-ocr(8001) 不受影响：健康 200。

## 3. 端到端用法（200 DPI）

**第 1 步**：PDF → 200 DPI PNG
```bash
# WSL 内（用 pymupdf venv）
wsl -d Ubuntu -- bash /mnt/c/Users/justin/Documents/工程量清单编制/render_pdf.sh "<pdf_wsl_path>" 0 "<out_png_wsl_path>" 200
```
（转换脚本 `pdf_to_png.py`；渲染结果示例：EL-01 电气设计说明 200dpi → 6700×4756 PNG）

**第 2 步**：OCR → Markdown
```bash
wsl -d Ubuntu -- bash /mnt/c/Users/justin/Documents/工程量清单编制/run_pocr.sh "<image_wsl_path>|<image_windows_path>" "<out_md_windows_path>"
```
（客户端已内置 LOC token 清洗，输出是干净 Markdown）

## 3.1 批量流水线（最终口径：0.55 显存 + 8张/批 = 12.67s/张，确定性高置信度）

`PaddleOCRVL.predict` **支持批量**：可传 **list** 或多图**目录**（目录自动扫描所有图片）。

**显存参数已调优**：`p-ocr-serve.service` 的 `--gpu-memory-utilization` 从 **0.4 → 0.55**（o-ocr 已删、p-ocr 独占显卡）。KV cache 从 54,832 → 124,384 tokens，最大并发 3.35× → 7.59×。

**确定性口径（temperature=0）实测**：

| 批量 | 均摊（确定性） |
|------|------|
| 单张串行 | ~22s/张 |
| 批量 5 张 | ~11.6s/张 |
| **批量 8 张（最优）** | **12.67s/张** |
| 批量 12 张 | 24.6s/张（更慢） |

**结论**：**8 张/批是混合管线最优点**。12 张反而变慢——因为瓶颈在 **native 预处理（PP-DocLayoutV3/方向/畸变）的显存**，不在 vLLM KV cache；批过大时 native 预处理挤兑显存。所以调高显存到 0.55 的意义是"8张批更稳、KV 不紧张"，而非"批更大"。

**40 张实测**（0.55 显存 + 8张/批 + temperature=0）：总 549.2s（约 9.2 分钟），均摊 13.73s/张，40/40 全出，vLLM 健康、无 OOM。

批量脚本（均用 native+vLLM 混合管线）：
- `convert_40.py` —— 从 `needs_ocr.json` 的 40 个 PDF 渲染 200dpi PNG 到 `C:\ocr_job\png`
- `run_40.py <batch=8>` —— 分批(默认8张)目录输入，输出到 `C:\ocr_job\out40`，统计 s/img
- `pocr_hybrid.py <图> [outdir]` —— 单张入口
- `batch_ocr_dir.py <dir> [outdir]` —— 目录批量入口（已加 temperature=0）

> **高置信度（重要）**：混合管线走 vLLM server 时，**必须显式传 `temperature=0.0`（贪心解码）**，否则 vLLM 端默认随机采样 → **同一张图多次跑结果不同（非确定性）**。已实测：不传 temperature 时 `IDENTICAL: False`（diff 7 行），传 `temperature=0.0` 后 `IDENTICAL: True`（完全可复现）。所有脚本都已加：
> `temperature=0.0, top_p=1.0, repetition_penalty=1.1, max_new_tokens=8192`。
>
> **已知限制**：纯图形图纸（总平面图/点位图/战时平面图）文字极少，OCR 提取出的文本偏短属正常（图纸性质决定）；这类图的图形信息（墙线/尺寸线）需走 CAD 文本解析，OCR 不适用。
>
> **PDF 性质分类（40 张实测）**：16 个真扫描件（图片型，OCR 有效）；24 个矢量 CAD 导出 PDF（无文字层、无图片，OCR 拿不到文字，需回 DWG 源文件走 cad-text-parser）。

## 4. 输出质量说明

- PaddleOCR-VL 原生会输出 `<|LOC_xxx|>` **坐标回归 token**（布局元数据，非可读文本）。
- 已在客户端脚本 `ocr()` 里过滤：`re.sub(r"<\|LOC_\d+\|>", "", raw)` + 清理空行。
- 验证结果：EL-01 图纸识别出「设计依据 / 设计范围 / 电缆管线敷设 / 配电装置 / 接地系统」章节 + GB 编号、IP 防护等级、埋深、接地电阻等数据。

## 5. 排障速查

| 命令 | 作用 |
|------|------|
| `systemctl is-active p-ocr-serve` | 服务是否在跑 |
| `wsl -d Ubuntu -- journalctl -u p-ocr-serve -n 50` | 看启动/运行日志 |
| `curl http://127.0.0.1:8001/health` | 健康检查（200=OK） |
| `systemctl status p-ocr-serve` | 单元状态 |

如果 8001 起不来，多半是**显存不够**（vLLM logs 出现 `No available memory for the cache blocks`）——确认 o-ocr 已被禁停，必要时重启 WSL（`wsl --shutdown`）。

## 6. 工具文件（工作区）

- `render_pdf.sh` + `pdf_to_png.py` —— PDF→PNG 200dpi
- `run_pocr.sh` —— 调 p-ocr 客户端
- `wait_pocr.bash` —— 轮询 p-ocr 就绪（后台任务）
- `p-ocr-skill/` —— 技能本体（SKILL.md + 客户端脚本副本）
